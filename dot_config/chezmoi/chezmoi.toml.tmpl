# Chezmoi self-referential template. Use this format and fill in all values
#	There will be errors for any missing data

[data]
computer_name = "{{ .computer_name }}"
email = "{{ .email }}"
homebrew_prefix = "{{ .homebrew_prefix }}"
# This is now co-opted for 'cheat' (use with `cheatz` alias)
pman_doc_path = "{{ .pman_doc_path }}"

[data.github]
email = "{{ .github.email }}"
signing_key = "{{ .github.signing_key }}"
gpg_program = "{{ .github.gpg_program }}"

[edit]
command = "subl"
args = ["--wait", "--add"]

[diff]
pager = "delta"

[merge]
command = "delta"
